var nums=[1,3,5,6];
var target=prompt("Input the targated Element");
for(var i=0;i<nums.length;i++) {
    if(nums[i]==target) {
        break;
    }
}
document.write("found the element at position = ",i);
